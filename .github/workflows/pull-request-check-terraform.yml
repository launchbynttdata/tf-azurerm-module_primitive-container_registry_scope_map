name: "Check Terraform"

on:
  pull_request:
    types: [ opened, reopened, synchronize, ready_for_review ]
    branches: [ main ]

jobs:
  check:
    name: "Reusable Workflow"
    permissions:
      contents: read
      pull-requests: read
      id-token: write
      statuses: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-terraform-check.yml@c57235a204b77b6622a0e99fe29dca5eecf80b2f
    with:
      auth_method: ${{ startsWith(github.event.repository.name, 'tf-aws') && 'aws' ||
        startsWith(github.event.repository.name, 'tf-az') && 'azure' || '' }}
    secrets: inherit # pragma: allowlist secret
